{
  "name": "SecureVaultItem",
  "description": "Encrypted credentials and sensitive keys",
  "properties": {
    "label": { "type": "string", "description": "Display label for the item" },
    "category": { "type": "string", "description": "Category (e.g., API Key, Login, Wallet)" },
    "username_hint": { "type": "string", "description": "Associated username or account ID" },
    "encrypted_payload": { "type": "string", "description": "AES-encrypted JSON payload" },
    "permission_scope": { "type": "string", "description": "Which departments can access this" },
    "last_accessed": { "type": "string", "description": "ISO timestamp of last access" }
  }
}